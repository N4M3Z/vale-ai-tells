# Vale config for the corpus measurement. Only the fork's own styles, so
# the gap report measures ai-tells and not Google or proselint. Every rule
# at its own level; the report reads levels, it does not filter on them.
StylesPath = ../styles
MinAlertLevel = suggestion

[*.md]
BasedOnStyles = ai-tells, ai-tells-experimental
