# The corpus is model output kept as evidence. It is measured by
# bench/scripts/report.py through bench/vale.ini, never linted as prose,
# so an editor hook or a commit hook does not block or rewrite it.
StylesPath = ../../styles
MinAlertLevel = error

[*.md]
BasedOnStyles =
